# TODO

- [ ] Consider using **lazy-tmux** as an alternative to the existing
      `tmux-resurrect`/`tmux-continuum` configuration (see devtools)
      to persist tmux sessions between restarts.
      https://lazy-tmux.xyz/tmux-setup
